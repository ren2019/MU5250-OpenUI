# 实时诊断页设计原型

问题：实时排障应以共同时间轴、异常事件，还是当前状态为主入口？

这是可丢弃的 UI 原型，保存在 `prototype/realtime-diagnostics` 分支。中文版基线已先保存为本地提交 `fabb0e4`；没有推送或部署设备。

## 启动与比较

在 `web-app` 目录运行 `npm run prototype:diagnostics`，打开 http://127.0.0.1:8089/?demo=1&prototype=diagnostics&variant=A 。如需登录，密码填 `demo`。

启动器使用本地模拟后端（127.0.0.1:9090），已有模拟服务则复用；前端仅监听本机。原型入口只在开发环境和 localhost/127.0.0.1 下启用。

| 方案 | 核心结构 | 适合回答的问题 |
| --- | --- | --- |
| A 时间轴对照 | 七条曲线上下排列，共用时间范围、联动光标，侧栏显示变化 | 掉速时，信号、小区、温度是否同时变化？ |
| B 事件排障 | 左侧选择事件，右侧查看关键曲线和核对线索 | 某次异常发生前后有什么变化？ |
| C 实时盯盘 | 当前状态概览、紧凑指标网格、变化记录 | 现在运行如何，哪些指标开始偏离？ |

用底部左右按钮或键盘方向键循环切换，也可将 URL 的 `variant` 改为 B/C。方案切换保留当前场景、采样和时间范围；进入 B 会优先聚焦事件。输入控件内的方向键不切换方案。

共有操作：5/15/30/60 分钟窗口、暂停/继续、返回最新读数、NR/LTE 选择、模拟 CSV 导出。三个场景分别演示小区切换与掉速、温度上升、采集缺口。刷新会重置内存数据。

## 结论与边界

设计建议：以 A 为后续基础，并考虑吸收 B 的事件聚焦方式。A 能直接对照多个指标的先后关系，B 缩短定位单次异常的路径，C 更适合持续观察。这个判断尚未经过用户选择验证，暂不将任何一版提升为正式功能。

全部曲线、事件、载波信息来自确定性的模拟数据；页面每 3 秒推进模拟采样，保留最多约 60 分钟。温度 10 秒等标注是未来采样设计目标，当前模拟器统一生成样本。真实接口支持、缓存周期及实际设备行为尚未在本原型中验证。长时间运行后，初始事件可能移出当前窗口，可切换场景重新演示。

缺失样本显示断线和“无读数”，不补零；温度与掉速同时出现不构成热降频结论。延迟、丢包、抖动明确列为待补充数据。诊断数据仅存内存，导出文件名明确包含 PROTOTYPE-simulated。

## 已检查

- 三方案切换、URL 状态、键盘切换、定时刷新、暂停、事件联动、采集缺口、载波和窗口选择。
- 390 × 844 手机布局与桌面布局；手机 C 方案无横向溢出。
- 单命令启动、TypeScript/Vite 构建、ESLint 通过；浏览器未捕获 error/warn。
- 正式构建文件与原型前的中文版构建逐文件一致，原型代码未进入发布产物。
- React Doctor：8 条提示，其中 7 条为既有维护性问题；新增提示是键盘监听 effect 随回调重新订阅。检查确认监听器有对称清理，无堆积；本轮不为一次性原型增加优化抽象。
- 按 prototype 技能要求未新增自动测试；CSV 已实现，未作为正式数据导出验收。

截图：[A 时间轴](images/diagnostic-prototype/A-timeline.png)、[B 事件排障](images/diagnostic-prototype/B-events.png)、[C 实时盯盘](images/diagnostic-prototype/C-live.png)。
